{
  "verification_timeout": {
    "description": "验证总超时时间（秒）",
    "type": "int",
    "default": 300,
    "hint": "新成员必须在此时间内完成验证，否则将被踢出群。默认 300 秒（5 分钟）。"
  },
  "kick_delay": {
    "description": "验证超时后踢出延迟（秒）",
    "type": "int",
    "default": 5,
    "hint": "发送'验证超时'消息后，等待多少秒再执行踢人操作。默认 5 秒。"
  },
  "new_member_prompt": {
    "description": "新成员入群提示语",
    "type": "string",
    "default": "{at_user} 欢迎加入本群！请在 {timeout} 分钟内回答下面的问题以完成验证：\n{question}",
    "hint": "用户入群时的提示语。可用变量: {at_user}, {question}, {timeout} (分钟)。"
  },
  "welcome_message": {
    "description": "验证成功提示语",
    "type": "string",
    "default": "{at_user} 验证成功，欢迎你的加入！",
    "hint": "用户验证成功后发送的消息。可用变量: {at_user}。"
  },
  "wrong_answer_prompt": {
    "description": "答案错误提示语",
    "type": "string",
    "default": "{at_user} 答案错误，请重新回答验证。这是你的新问题：\n{question}",
    "hint": "用户回答错误后发送的消息，之后会自动生成新问题。可用变量: {at_user}, {question}。"
  },
  "failure_message": {
    "description": "验证超时提示语",
    "type": "string",
    "default": "{at_user} 很抱歉，您对验证的响应超时，您将在 {countdown} 秒后被请出本群。",
    "hint": "用户因超时而验证失败后，踢出前发送的消息。可用变量: {at_user}, {countdown} (踢出延迟秒数)。"
  },
  "kick_message": {
    "description": "最终踢出提示语",
    "type": "string",
    "default": "{at_user} 很抱歉，您因未在规定时间内完成验证，已被请出本群。",
    "hint": "用户被踢出后发送的公开消息。可用变量: {at_user}。"
  },
  "max_wrong_answers": {
    "description": "最大错误回答次数",
    "type": "int",
    "default": 5,
    "hint": "用户回答错误的最大次数，超过此次数将被踢出群。默认 5 次。"
  },
  "too_many_wrong_message": {
    "description": "错误次数过多踢出提示语",
    "type": "string",
    "default": "{at_user} 很抱歉，你已连续回答错误 {count} 次，将被请出本群。",
    "hint": "用户回答错误次数过多时发送的消息。可用变量: {at_user}, {count} (错误次数)。"
  },
  "api_base_url": {
    "description": "极验验证后端 API 基础地址",
    "type": "string",
    "default": "",
    "hint": "极验验证后端服务的 API 基础地址，注意末尾不要带斜杠，例如 https://example.com，若不想自建服务想使用作者的服务，请加群获取：195260107"
  },
  "api_key": {
    "description": "API 访问密钥",
    "type": "string",
    "default": "",
    "hint": "极验验证后端服务的 API 访问密钥。若使用作者的服务，请加群私信群主获取密钥：195260107"
  },
  "enable_geetest_verify": {
    "description": "启用极验验证",
    "type": "bool",
    "default": false,
    "hint": "是否启用极验验证。如果启用，将使用极验验证后端服务进行验证；如果禁用，将使用本地数学题验证。"
  },
  "enable_level_verify": {
    "description": "启用等级验证",
    "type": "bool",
    "default": false,
    "hint": "是否启用等级验证。如果启用，QQ等级大于等于最低等级的用户将自动跳过验证。"
  },
  "min_qq_level": {
    "description": "最低QQ等级",
    "type": "int",
    "default": 20,
    "hint": "启用等级验证时，用户QQ等级达到此等级将自动跳过验证。默认 20 级。"
  },
  "verify_delay": {
    "description": "入群验证延时（秒）",
    "type": "int",
    "default": 3,
    "hint": "新成员入群后，延时多少秒再发送验证消息。设置为 0 表示立即发送。默认 3 秒。"
  },
  "group_configs": {
    "description": "群级别配置",
    "type": "template_list",
    "hint": "为不同的群配置不同的验证参数。可以在 WebUI 上快速添加和编辑群配置。",
    "templates": {
      "default_config": {
        "name": "默认配置",
        "hint": "适用于大多数群的标准配置",
        "items": {
          "group_id": {
            "description": "群号",
            "type": "int",
            "hint": "要配置的群号"
          },
          "enabled": {
            "description": "启用验证",
            "type": "bool",
            "default": false,
            "hint": "是否启用该群的入群验证。默认关闭。"
          },
          "verification_timeout": {
            "description": "验证总超时时间（秒）",
            "type": "int",
            "default": 300,
            "hint": "新成员必须在此时间内完成验证，否则启动踢出流程。默认 300 秒（5 分钟）。"
          },
          "max_wrong_answers": {
            "description": "最大错误回答次数",
            "type": "int",
            "default": 5,
            "hint": "用户回答错误的最大次数，超过此次数将被踢出群。默认 5 次。"
          },
          "enable_geetest_verify": {
            "description": "启用极验验证",
            "type": "bool",
            "default": false,
            "hint": "是否启用极验验证。如果启用，将使用极验验证后端服务进行验证；如果禁用，将使用本地数学题验证。"
          },
          "enable_level_verify": {
            "description": "启用等级验证",
            "type": "bool",
            "default": false,
            "hint": "是否启用等级验证。如果启用，QQ等级大于等于最低等级的用户将自动跳过验证。"
          },
          "min_qq_level": {
            "description": "最低QQ等级",
            "type": "int",
            "default": 20,
            "hint": "启用等级验证时，用户QQ等级达到此等级将自动跳过验证。默认 20 级（皇爵）。"
          },
          "verify_delay": {
            "description": "入群验证延时（秒）",
            "type": "int",
            "default": 3,
            "hint": "新成员入群后，延时多少秒再发送验证消息。设置为 0 表示立即发送。默认 3 秒。"
          }
        }
      }
    }
  }
}